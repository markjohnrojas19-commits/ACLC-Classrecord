# Progress Log

> Append-only log. Every meaningful decision, action, and verification goes here as a bullet point.
> Keep entries short and chronological. The format is intentionally flat — no sections, no headings, no nesting.
>
> **Conventions:**
> - **Decision:** ... — a choice was made (record what and why)
> - **Action:** ... — something was changed in the codebase or environment
> - **Verified:** ... — the user confirmed the change works on their machine
> - **Rule:** ... — a new project-wide rule was added (also reflect in `CLAUDE.md`)
> - **Refactor:** ... — code was reshaped without changing behavior
> - **Bugfix:** ... — an incorrect behavior was corrected
> - **Issue:** ... — an unresolved problem to come back to
> - **Plan:** ... — the next thing to try

---

- Project initialized from `project-boilerplate`
- Created initial `CLAUDE.md`, `milestones.md`, `progress.md`, `ARCHITECTURE.md`
- **Decision:** Project is ACLC Class Record System — a Java Swing desktop app for managing students, subjects, grades, and attendance. Built in NetBeans IDE with MySQL (XAMPP) + JDBC.
- **Decision:** Tech stack confirmed — Java + Swing (GUI), MySQL + XAMPP (database), JDBC (connection), NetBeans IDE. No external frameworks.
- **Decision:** Primary quality attribute is **readability** — instructor will read the code.
- **Decision:** Architecture is 4-layer: Model -> DAO -> Service -> UI. Lower layers never reference upper layers.
- **Decision:** Grade computation weights stored as constants in `GradeConstants.java` so they're easy to change when instructor confirms the formula. Default: 30% Quiz, 30% Assignment, 40% Exam. Passing grade: 75.
- **Decision:** Feature tiers established — Must-have: Login, Student CRUD, Subject CRUD, Grade Management, Dashboard. Nice-to-have: Attendance, Search/Filter, Print. Deferred: PDF/Excel export, Calendar attendance.
- **Decision:** Database name is `aclc_classrecord_db` with tables: users, students, subjects, grades.
- **Decision:** 8 core milestones defined, from project setup through polish. 2 additional nice-to-have milestones (attendance, print).
- **Action:** Updated all md files (`CLAUDE.md`, `milestones.md`, `progress.md`, `ARCHITECTURE.md`) to reflect project-specific content.
- **Plan:** Next step is Milestone 1 — create NetBeans project and set up the MySQL database.
- **Action:** Step 1.1 — NetBeans Java Application project created at `ACLC-Classrecord/` subfolder (contains `build.xml`, `nbproject/`, empty `src/`).
- **Action:** Step 1.2 — Wrote `sql/schema.sql` with CREATE DATABASE, all four tables (users, students, subjects, grades), foreign keys, and a default admin user (admin / admin123).
- **Plan:** Next is Step 1.3 — run `schema.sql` in phpMyAdmin to create the database.
- **Verified:** Step 1.3 — user confirmed database and tables created in phpMyAdmin.
- **Verified:** Step 1.4 — user added MySQL Connector/J JAR to NetBeans project libraries.
- **Action:** Step 1.5 — Created `dao/DatabaseConnection.java` (connection utility with URL, user, password for localhost MySQL). Created `Main.java` entry point that calls `DatabaseConnection.getConnection()` and prints success/failure to console.
- **Verified:** Step 1.5 — user confirmed "Connected to database successfully!" prints to console. Database connection works.
- **Milestone 1 complete.** Project setup and database connection verified.
- **Action:** Step 2.1 — Created `model/Role.java` (enum: ADMIN, INSTRUCTOR) and `model/User.java` (4 fields: userId, username, password, role). User is immutable (getters only, no setters) since login returns a read-only snapshot.
- **Decision:** Role is a separate enum rather than a String field — gives compile-time safety and readable role checks.
- **Action:** Step 2.2 — Created `dao/UserDao.java` with `authenticate(username, password)` method. Uses PreparedStatement to query `users` table. Returns `User` object on match, `null` on failure. Extracts role string from DB and converts to `Role` enum via `Role.valueOf()`.
- **Action:** Step 2.3 — Already done. The `schema.sql` from Step 1.2 already inserts a default admin user (admin / admin123).
- **Action:** Updated `Main.java` to test authentication — runs both a valid and invalid login attempt and prints results to console.
- **Verified:** Steps 2.2 & 2.3 — authentication test printed expected output.
- **Action:** Steps 2.4 & 2.5 — Created `ui/LoginForm.java`. Uses nested BorderLayout + GridLayout for readable layout. Fields panel (GridLayout 2x2) holds username/password labels and fields. Button panel (FlowLayout) holds Login button. Login button calls `UserDao.authenticate()`, shows error dialog on failure, welcome dialog on success.
- **Decision:** Layout approach is nested simple layouts (BorderLayout + GridLayout + FlowLayout) — readable for instructor, handles resizing, no overengineering.
- **Action:** Updated `Main.java` to launch `LoginForm` via `SwingUtilities.invokeLater()` instead of the console test.
- **Verified:** Steps 2.4 & 2.5 — login form works. Valid credentials show welcome, invalid credentials show error, empty fields show warning.
- **Action:** Step 2.6 — Created `ui/DashboardForm.java` as a minimal shell (800x600 JFrame with welcome label). Updated `LoginForm.onLoginSuccess()` to open DashboardForm and dispose LoginForm. Removed the temporary welcome dialog.
- **Verified:** Step 2.6 — login opens dashboard, login window closes. Milestone 2 complete.
- **Action:** Steps 3.1, 3.2, 3.3 — Rewrote `ui/DashboardForm.java` with full layout. Header panel: welcome label (bold, 20pt) + logout button. Center: 2x2 grid of placeholder stat labels (Total Students, Total Subjects, Passed, Failed — all showing "0" for now). Bottom: navigation buttons (Students, Subjects, Grades) — not yet wired to forms. Logout creates a new LoginForm and disposes the dashboard.
- **Decision:** All three steps done together since they're all layout changes to the same file and individually trivial.
- **Plan:** User verifies dashboard layout + logout flow, then Milestone 3 is complete. Next: Milestone 4 — Student Management.
- **Verified:** Milestone 3 complete — dashboard layout and logout flow confirmed working.
- **Action:** Step 4.1 — Created `model/Student.java` with 7 fields (studentId, firstname, lastname, course, yearLevel, section, gender), constructor, getters, and setters. Mutable design chosen because students are actively edited via the UI form.
- **Decision:** Student is mutable (unlike User which is immutable) because the edit workflow requires updating fields on existing objects.
- **Action:** Step 4.2 — Created `dao/StudentDao.java` with 5 CRUD methods (add, getAll, update, delete, search) plus 2 private helpers (setStudentParameters, extractStudent). All use try-with-resources and PreparedStatement. Search uses LIKE on student_id, firstname, lastname.
- **Decision:** Extracted `StudentInputPanel` as a separate atom from `StudentForm` to respect class field limits. StudentInputPanel holds the 7 input fields (intrinsic to its role as an input panel). StudentForm holds only 4 fields (inputPanel, studentTable, studentDao, searchField).
- **Action:** Step 4.3 — Created `ui/StudentInputPanel.java` (7 input fields, methods: toStudent, fromStudent, clear, lockStudentId, hasEmptyFields). Created `ui/StudentForm.java` (header with back button, input panel, JTable with row selection, action buttons: Add/Edit/Delete/Clear/Search). Wired DashboardForm "Students" button to open StudentForm.
- **Verified:** Step 4.3 — StudentForm opens from dashboard, layout displays correctly, back button works.
- **Verified:** Steps 4.4-4.7 — All CRUD operations verified: Add inserts and refreshes table, Edit updates selected row, Delete confirms and removes, Search filters by name/ID. Edge cases (empty fields, no selection) show proper error messages.
- **Milestone 4 complete.** Full student CRUD works end-to-end with database persistence.
- **Action:** Step 4.2 — Created `dao/StudentDao.java` with 5 methods: `add()`, `getAll()`, `update()`, `delete()`, `search()`. All use try-with-resources and PreparedStatement. `search()` matches keyword against student_id, firstname, or lastname using LIKE. Two private helpers: `setStudentParameters()` (binds Student fields to INSERT statement) and `extractStudent()` (maps ResultSet row to Student object). Follows same patterns as `UserDao.java`.
- **Action:** Step 5.1 — Created `model/Subject.java` with 3 fields (subjectId, subjectCode, subjectName), constructor, getters, and setters. Mutable design (like Student) since subjects will be edited via the UI form.
- **Decision:** Subject is mutable (getters + setters) — same pattern as Student. The edit workflow requires updating fields on existing objects.
- **Action:** Step 5.2 — Created `dao/SubjectDao.java` with 5 methods: `add()`, `getAll()`, `update()`, `delete()`, `search()`. All use try-with-resources and PreparedStatement. `add()` omits `subject_id` since it's auto-increment. `search()` matches keyword against subject_code or subject_name using LIKE. One private helper: `extractSubject()` (maps ResultSet row to Subject object). Follows same patterns as `StudentDao.java`.
- **Decision:** No `setSubjectParameters()` helper needed (unlike StudentDao) — Subject only has 2 insertable fields, so inline binding is simpler and more readable.
- **Verified:** Step 5.2 — compiles successfully.
- **Action:** Step 5.3 — Created `ui/SubjectForm.java` with layout: header (title + back button), input panel (subject code + subject name in 1x4 GridLayout), JTable (3 columns: ID, Subject Code, Subject Name), and button panel (Add, Edit, Delete, Clear, Search — Clear wired, others pending Step 5.4). Row selection populates input fields. Table loads from database on open.
- **Decision:** No separate SubjectInputPanel needed — only 2 input fields, so SubjectForm stays at 5 total fields (within the 1-5 atom limit). Extracting a panel for 2 fields would be overengineering.
- **Action:** Wired DashboardForm "Subjects" button to open SubjectForm (same pattern as "Students" button).
- **Verified:** Step 5.3 — compiles and layout works. SubjectForm opens from dashboard, back button works.
- **Action:** Step 5.4 — Wired all CRUD buttons in `SubjectForm.java`: `handleAdd()` creates Subject with id=0 (auto-generated by DB), `handleEdit()` reads subjectId from table row to preserve identity, `handleDelete()` confirms via dialog then deletes by subjectId, `handleSearch()` filters by keyword or refreshes on empty. Added `hasEmptyFields()` and `showError()` helpers.
- **Verified:** Step 5.4 — All CRUD operations verified: Add inserts and refreshes table, Edit updates selected row, Delete confirms and removes, Search filters by code/name. Edge cases (empty fields, no selection) show proper error messages.
- **Milestone 5 complete.** Full subject CRUD works end-to-end with database persistence.
- **Action:** Step 6.1 — Created `util/GradeConstants.java` with 4 constants: `QUIZ_WEIGHT` (0.30), `ASSIGNMENT_WEIGHT` (0.30), `EXAM_WEIGHT` (0.40), `PASSING_GRADE` (75.0). Class is `final` with a private constructor to prevent instantiation — it's a pure constants holder.
- **Decision:** Used a simple constants class (Option A) over properties file or database config. Weights only change when instructor confirms the formula — a recompile moment, not a runtime change. Simplest approach for a school project.
- **Action:** Step 6.2 — Created `model/Grade.java` (6 fields: gradeId, studentId, subjectId, quiz, assignment, exam) and `model/ScoreResult.java` (2 fields: finalGrade, remarks). Mutable design (getters + setters) — same pattern as Student and Subject.
- **Decision:** Split grade data into two atoms: `Grade` holds the raw input scores, `ScoreResult` holds the computed outputs (finalGrade, remarks). The split reflects a real concern boundary — inputs vs. computed outputs. `GradeComputer` will take scores in and return a `ScoreResult`. Both atoms stay within field limits.
- **Action:** Step 6.3 — Created `service/GradeComputer.java` with 1 public method `compute(quiz, assignment, exam)` that returns a `ScoreResult`. Two private helpers: `computeWeightedAverage()` applies weights from `GradeConstants`, `determineRemarks()` returns "PASSED" or "FAILED" based on `PASSING_GRADE` threshold. Zero fields — pure computation, no state.
- **Action:** Step 6.4 — Created `model/GradeRecord.java` (2 fields: Grade + ScoreResult — composes both into one object representing a complete DB row). Created `dao/GradeDao.java` with 5 methods: `add(Grade, ScoreResult)`, `getAll()`, `update(Grade, ScoreResult)`, `delete(gradeId)`, `search(keyword)`. Two private helpers: `setGradeParameters()` and `extractGradeRecord()`. Follows same patterns as StudentDao and SubjectDao.
- **Decision:** Created `GradeRecord` as a composition class because the DB `grades` row maps to two model atoms (Grade + ScoreResult). The DAO needs to read/write both in a single row. GradeRecord is the external composition — neither Grade nor ScoreResult knows about the other.
- **Action:** Step 6.5 — Added `toString()` to `Student.java` ("ID - FirstName LastName") and `Subject.java` ("Code - Name") so JComboBox displays them readably. Created `ui/GradeInputPanel.java` (5 fields: studentBox, subjectBox, quizField, assignmentField, examField) with methods for getting selections, scores, population, and clearing. Created `ui/GradeForm.java` (5 fields: inputPanel, gradeTable, gradeDao, gradeComputer, searchField) with layout: header + back button, input panel, JTable (8 columns), button panel (Add/Edit/Delete/Clear/Search — only Clear wired for now). Wired DashboardForm "Grades" button to open GradeForm.
- **Decision:** Used `JComboBox<Student>` and `JComboBox<Subject>` directly instead of parallel lists for ID tracking. Adding `toString()` to the models is intrinsic (every Java object has a string representation) and eliminates the need for extra fields on the input panel.
- **Action:** Step 6.6 — Added live auto-compute to `GradeInputPanel`: new `resultLabel` field (6 fields total — at warning level but justified, all intrinsic), `addScoreChangeListener(Runnable)` using DocumentListener on all 3 score fields, `updateResult()`, `setScores()`, `selectStudent()`, `selectSubject()`. Updated `GradeForm` with full CRUD wiring: `handleAdd()` builds Grade + computes ScoreResult + saves via GradeDao, `handleEdit()` updates by gradeId, `handleDelete()` with confirmation dialog, `handleSearch()` filters by student ID, `loadSelectedGrade()` populates inputs on row selection, `computeAndDisplay()` triggered on every keystroke in score fields. Table currently shows raw IDs (student_id, subject_id) — Step 6.7 will enhance with readable names.
- **Decision:** Auto-compute uses DocumentListener (fires on every keystroke) rather than requiring a button click. This gives instant feedback — the user sees the computed grade update as they type.
- **Action:** Step 6.7 — Enhanced `GradeForm` JTable to show readable names instead of raw IDs. "Student" column shows "STU001 - John Doe" (via Student.toString()), "Subject" column shows "CS101 - Programming" (via Subject.toString()). Added `currentRecords` field (List<GradeRecord>) so `loadSelectedGrade()` can retrieve raw IDs by row index without parsing display strings. Added `buildStudentNameMap()` and `buildSubjectNameMap()` helpers that create ID→display-name maps from the database.
- **Decision:** Used a parallel `currentRecords` list rather than parsing IDs from display strings. Row index in the table matches the index in currentRecords, so `currentRecords.get(selectedRow)` gives direct access to the raw Grade data. Clean and non-fragile.
- **Milestone 6 complete.** Grade management works end-to-end: auto-compute with live display, full CRUD, readable JTable with student/subject names.
- **Action:** Step 7.1 — Created `dao/DashboardDao.java` with 4 public methods: `countStudents()`, `countSubjects()`, `countPassed()`, `countFailed()`. All delegate to a single private `executeCount(sql)` helper that runs a `SELECT COUNT(*)` query and returns the result. Zero fields — pure query methods. Follows the same try-with-resources + PreparedStatement pattern as other DAOs.
- **Decision:** Dashboard counts live in a dedicated `DashboardDao` rather than being added to existing CRUD DAOs. The counts are a display/aggregation concern, not intrinsic to student/subject/grade CRUD. Keeps existing DAOs focused and follows atomic design.
- **Action:** Steps 7.2 & 7.3 — Created `ui/DashboardStatsPanel.java` (5 fields: dashboardDao + 4 JLabels). Has `refresh()` method that queries `DashboardDao` and updates all 4 labels. Updated `DashboardForm` to compose `DashboardStatsPanel` instead of creating labels inline. Removed old `createStatsPanel()` and `createStatLabel()` methods. `statsPanel.refresh()` is called in the constructor so stats load automatically when the dashboard opens.
- **Decision:** Extracted `DashboardStatsPanel` from `DashboardForm` to keep both classes within field limits. Same pattern as `StudentInputPanel` and `GradeInputPanel`. DashboardForm stays at 2 fields (currentUser, statsPanel).
- **Verified:** Steps 7.2 & 7.3 — dashboard shows live counts from database. Counts update when navigating back after adding data.
- **Milestone 7 complete.** Dashboard displays accurate, live summary statistics.
- **Action:** Step 8.2 — Added numeric validation. `StudentInputPanel.hasInvalidYearLevel()` rejects values outside 1-4. `GradeInputPanel.hasInvalidScores()` rejects non-numeric or out-of-range (0-100) values via private `isValidScore()` helper. Wired both checks into `handleAdd()` and `handleEdit()` in `StudentForm` and `GradeForm` — validation runs after the empty-field check, before building the domain object.
- **Decision:** Validation methods live on the input panels (they know their fields), error dialogs live on the forms (they know about the UI). Panels are query-only atoms — they answer "is this valid?" but don't show dialogs.
- **Note:** Step 8.3 (delete confirmations) was already implemented during Milestones 4-6. Marked complete with no changes needed.
- **Action:** Step 8.4 — `DashboardDao.executeCount()` returns -1 on error (was 0). `DashboardStatsPanel.updateLabel()` displays "Error" when count is negative.
- **Action:** Step 8.1 — Created `util/StyleConstants.java` with standardized fonts (TITLE_FONT 18pt, BODY_FONT 16pt, SMALL_BOLD_FONT 13pt), borders (HEADER, INPUT, BUTTON, TABLE, STATS), and gaps (GRID_H_GAP, GRID_V_GAP, BUTTON_GAP). Updated all 7 UI files (DashboardForm, DashboardStatsPanel, StudentForm, StudentInputPanel, SubjectForm, GradeForm, GradeInputPanel) to reference StyleConstants. Removed hard-coded Font and BorderFactory calls. Dashboard title standardized from 20pt to 18pt; dashboard nav gap from 20px to 10px; dashboard padding aligned with CRUD forms. LoginForm left unchanged (unique centered layout).
- **Decision:** Used `StyleConstants` (Option B) over inline value alignment. Mirrors `GradeConstants` pattern — single source of truth, no magic numbers. Changing the app's look requires editing one file.
- **Verified:** Step 8.1 — Dashboard and all forms display with consistent styling. Live stats confirmed working (4 students, 3 subjects).
- **Milestone 8 complete.** App is polished: consistent styling via StyleConstants, numeric validation on all inputs, delete confirmations on all CRUD forms, error handling for database failures.
- **Action:** Step 8.4 — Changed `DashboardDao.executeCount()` to return -1 on `SQLException` (was 0). Added `updateLabel()` helper to `DashboardStatsPanel` that displays "Error" when count is negative, otherwise shows the number. CRUD forms already had user-friendly error handling from earlier milestones.
- **Decision:** Used -1 sentinel from DAO rather than throwing exceptions. Consistent with the existing DAO pattern (catch internally, return a status). Panel checks for negative values — simple and readable.
- **Decision:** Milestone 9 — Major architectural redesign: replace fixed quiz/assignment/exam grade model with flexible assessment-based grading. Students organized by section using JTabbedPane. Grading seasons (Prelim, Midterm, Pre-Final, Final) with arbitrary assessment types per season.
- **Decision:** Chose JTabbedPane for section organization over JComboBox filter or accordion. Natural Swing component, instant switching, all sections visible as tab labels.
- **Decision:** Chose flexible `assessments` table over extending the old `grades` table with a period column. The old model (fixed quiz/assignment/exam columns) couldn't support arbitrary assessment types like "Unit Test A" or "Project". New model: one row per assessment score.
- **Decision:** Grade computation changed from weighted average (30% quiz, 30% assignment, 40% exam) to simple average of all assessment scores in a season. Remarks still based on 75.0 passing threshold.
- **Decision:** GradingSeason is a Java enum (matching the existing Role enum pattern). Grading seasons are fixed in ACLC's academic structure — enum gives compile-time safety.
- **Action:** Step 9.1 — Created `model/GradingSeason.java` (enum with toDisplayName, toDbValue, fromDbValue converters) and `model/Assessment.java` (6 fields: assessmentId, studentId, subjectId, season, assessmentName, score).
- **Action:** Step 9.2 — Replaced `grades` table with `assessments` table in schema.sql. New table: assessment_id, student_id (FK), subject_id (FK), season (ENUM), assessment_name, score. UNIQUE constraint on (student_id, subject_id, season, assessment_name).
- **Action:** Step 9.3 — Created `dao/AssessmentDao.java` with 6 methods: add, getAll, getBySeason, update, delete, search. Search matches on student_id or assessment_name. Private helpers: setParameters, extractAssessment, executeQuery.
- **Action:** Step 9.4 — Rewrote `service/GradeComputer.java`: single public method `computeAverage(List<Assessment>)` returns ScoreResult. Private helpers: calculateAverage, determineRemarks. Simplified `util/GradeConstants.java` to PASSING_GRADE + MIN_SCORE + MAX_SCORE (removed weighted constants).
- **Action:** Step 9.5 — Created `ui/SectionTablePanel.java`: reusable JTable panel with styled dark header, alternating row colors, configurable via StyleConstants. Methods: populate(List<Student>), getSelectedRow, getValueAt, getTable.
- **Action:** Step 9.6 — Refactored `ui/StudentForm.java` to use JTabbedPane. "All" tab shows all students, dynamic tabs per section extracted from student data. Each tab uses SectionTablePanel. Selecting a row in any tab populates input panel. Search rebuilds tabs from filtered results.
- **Action:** Step 9.7 — Created `ui/AssessmentInputPanel.java` (6 fields: studentBox, subjectBox, seasonBox, assessmentNameField, scoreField, resultLabel). Refactored `ui/GradeForm.java` with season JTabbedPane (4 tabs: Prelim, Midterm, Pre-Final, Final). Each tab shows filtered assessments + season average label. Season average computed via GradeComputer and displayed with PASSED/FAILED coloring.
- **Action:** Step 9.8 — Updated `dao/DashboardDao.java`: countPassed/countFailed now use subquery `SELECT ... GROUP BY student_id, subject_id HAVING AVG(score) >= 75.0`. Added countAssessments. Updated `util/StyleConstants.java` with modern color palette (PRIMARY blue, TABLE_HEADER dark, alternating row colors, SUCCESS green, DANGER red, SEASON_AVERAGE_BG).
- **Action:** Step 9.9 — Deleted old files: Grade.java, GradeRecord.java, GradeDao.java, GradeInputPanel.java. Updated DashboardForm header with styled colors. Updated milestones.md, progress.md, ARCHITECTURE.md.
- **Milestone 9 complete.** Students organized by section (JTabbedPane), grades organized by grading season with flexible assessment types, modern UI styling with color palette.
- **Bugfix:** Deleting a student with existing grades/assessments failed with foreign key constraint error (`Cannot delete or update a parent row`). Fixed `StudentDao.delete()` to delete related rows from `grades` and `assessments` tables before deleting the student. Three private helpers added: `deleteRelatedGrades()`, `deleteRelatedAssessments()`, `deleteStudent()` — all sharing the same connection.
- **Verified:** Student delete works for students with and without existing grades/assessments.
- **Decision:** Milestone 10 scope expanded beyond just attendance — added enrollment as a prerequisite. Enrollment links students to subjects via a junction table, so attendance and grade forms can filter to only enrolled students.
- **Action:** Step 10.1 — Added `enrollments` table (enrollment_id PK, student_id FK, subject_id FK, UNIQUE student+subject) and `attendance` table (attendance_id PK, student_id FK, subject_id FK, date, status ENUM, UNIQUE student+subject+date) to schema.sql.
- **Action:** Step 10.2 — Created `model/AttendanceStatus.java` (enum: PRESENT, ABSENT, LATE, EXCUSED with display/db converters), `model/Attendance.java` (5 fields), `model/Enrollment.java` (3 fields).
- **Action:** Step 10.3 — Created `dao/EnrollmentDao.java` with enroll, unenroll, unenrollByStudentAndSubject, isEnrolled, getBySubject, getStudentsBySubjectAndSection (JOIN query returning Student objects), getSectionsBySubject.
- **Action:** Step 10.4 — Created `dao/AttendanceDao.java` with add, update, saveOrUpdate (upsert), getBySubjectAndDate, getByStudent, countByStatus, countTodayPresent, countTodayTotal.
- **Decision:** AttendanceDao uses `saveOrUpdate()` pattern — checks if record exists, then inserts or updates. Avoids duplicate key errors when re-marking attendance for the same date.
- **Action:** Step 10.5 — Created `ui/EnrollmentForm.java` + `ui/EnrollmentFilterPanel.java`. Checkbox table by subject+section, bulk enroll/unenroll buttons. Save compares checkbox state to DB enrollment state and enrolls/unenrolls accordingly.
- **Action:** Step 10.6 — Created `ui/AttendanceForm.java` + `ui/AttendanceFilterPanel.java`. Subject+section+date filters, status dropdowns per student row, "Mark All Present" bulk button. Only enrolled students appear (via EnrollmentDao.getStudentsBySubjectAndSection).
- **Action:** Step 10.7 — Added "Enrollment" and "Attendance" navigation buttons to DashboardForm.
- **Action:** Step 10.8 — Updated DashboardStatsPanel: added `enrolledLabel` (from DashboardDao.countEnrolled) and `attendanceLabel` ("Today's Attendance: X / Y" from countTodayPresent/countTodayTotal). Grid expanded to 3x2. Updated DashboardDao with countEnrolled, countTodayPresent, countTodayTotal methods.
- **Milestone 10 complete.** Enrollment and attendance tracking with section-based workflow. Only enrolled students appear in attendance forms. Dashboard shows enrollment count and today's attendance summary.
- **Action:** Step 12.1 — Added season weight constants to `util/GradeConstants.java`: PRELIM_WEIGHT (0.20), MIDTERM_WEIGHT (0.20), PRE_FINAL_WEIGHT (0.20), FINAL_WEIGHT (0.40). These follow ACLC's standard weighting where the Final season counts double. Named constants match the existing pattern in the file.
- **Action:** Step 12.2 — Added `computeFinalGrade(Map<GradingSeason, List<Assessment>>)` to `service/GradeComputer.java`. Iterates all 4 seasons, computes each season's simple average via existing `calculateAverage()`, multiplies by the season weight from `getSeasonWeight()` helper, sums into a weighted total. Returns `ScoreResult` with weighted grade + PASSED/FAILED remarks. Also added null-safety to `calculateAverage()` for seasons with no assessments (treated as 0.0).
- **Decision:** Missing seasons are treated as 0.0 average — if a student has no assessments for a season, that season contributes 0 to the weighted total. This is honest: incomplete work means a lower grade. Individual season averages remain visible in the season tabs for context.
- **Action:** Steps 12.3 & 12.4 — Added "Final Grade" tab (5th tab) to `ui/GradeForm.java`. Tab shows one row per student-subject pair with columns: Student, Subject, Prelim avg, Midterm avg, Pre-Final avg, Final avg, Weighted Final Grade, Remarks. Assessments are grouped by student-subject key using `groupByStudentSubject()` (LinkedHashMap preserves insertion order). Each season average computed via `formatSeasonAverage()` helper. Weighted final grade computed by `GradeComputer.computeFinalGrade()`. Custom `createFinalGradeRenderer()` color-codes the Final Grade and Remarks columns — green (SUCCESS) for PASSED, red (DANGER) for FAILED. Alternating row colors maintained. Guard added to `getSelectedAssessment()` so clicking rows in the Final Grade tab returns null (read-only summary, not editable). No new fields added to GradeForm — the tab lives inside the existing `seasonTabs` JTabbedPane.
- **Action:** Step 12.5 — Updated `dao/DashboardDao.java` `executePassFailCount()` to use weighted final grade instead of simple `AVG(score)`. SQL now computes `COALESCE(AVG(CASE WHEN season = 'Prelim' THEN score END), 0) * ? + ...` for each season, using parameterized weights from `GradeConstants`. COALESCE handles missing seasons (NULL → 0), matching the Java-side behavior. Dashboard passed/failed counts now reflect the same weighted formula as the Final Grade tab.
- **Milestone 12 complete.** Instructor sees a combined final grade per student per subject with configurable season weights (Prelim 20%, Midterm 20%, Pre-Final 20%, Final 40%). Dashboard passed/failed counts use the same weighted formula.
